node_modules/
.DS_Store
*.log
CLAUDE.md
README.md
DAILY-INTAKE.md
WORKSPACE-HANDOFF.md

# Environment variables and secrets
.env
*-google-drive.json
.credentials.enc
.env.local
.env.*.local
*session-token*
*.token

# Generated files
latest_inventory_chart.html

# NPM cache
.npm/
npm-debug.log*
package-lock.json

# Editor backup files
*.save

# Claude Code local settings (contains session tokens)
.claude/

# Google Cloud service account keys (NEVER commit)
*-service-account*.json
*-gmail-key-*.json
harvrealtor-*.json
*google-drive*.json

# Any file matching common credential patterns
*credential*.json
*-creds.json
service-account*.json

# Local-only, intentionally untracked on both Mac and VPS (git-sync leaves them alone).
# NOTE: .gitignore has no trailing-comment support, so each pattern gets its own line.
# /daily-report skill (symlinked from ~/.claude/skills; Drive preserves the Mac copy)
daily-report-skill/
# broadcast safety net (see CLAUDE.md "Stage 4.5b")
broadcast-backstop.js

# once-per-day stamp for the harvrealtor.net rebuild trigger (local state)
.net-rebuild-last-date
